---
title: Unity 属性动画导出
description: 将 Blender 动画曲线转换为 Unity .anim 文件。
---

# Unity 属性动画导出

## 入口与输入

说明支持的数据块、Action/槽选择和层级根对象。

## 映射

记录 Blender 数据路径到 Unity 属性路径的映射、对象路径、旋转表示、坐标转换和插值规则。

## 导出

说明帧范围、采样率、循环设置、输出路径和文件覆盖。用一个短动画验证 Unity 导入结果。
