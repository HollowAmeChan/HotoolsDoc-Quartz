![[Pasted image 20260730183821.png]]
# 形态键传递

## 工作原理

以源物体形态键为输入，通过顶点匹配把位移传给目标物体。支持点序、世界坐标和 UV 等匹配方式；具体名称与参数以当前面板为准。

## 点序匹配

要求两边顶点数量和顺序完全一致，速度快且可做到无损传递。分离后重新合并等操作可能改变点序，即使顶点数相同也会产生错误结果。

## 坐标与 UV 匹配

从初始半径开始搜索，按迭代次数扩大范围并选择匹配点。

## 相对偏移与绝对位置

默认通常传递相对 Basis 的偏移；绝对位置模式则把源形态位置直接作为目标。
